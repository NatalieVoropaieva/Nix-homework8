let login;
let message = login == 'Співробітник'
                ? 'Привіт'
                :  login == 'Директор'
                ? 'Добрий день'
                : login == ''
                ? 'Відсутній логін'
                : '';